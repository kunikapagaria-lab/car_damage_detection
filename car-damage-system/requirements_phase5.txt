# Phase 5 — hardening, reports, auth, tasks
# Install on top of requirements_backend.txt:
#   pip install -r requirements_backend.txt -r requirements_phase5.txt

# PDF generation
weasyprint==62.3
Jinja2==3.1.4
qrcode==7.4.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Async tasks
celery[redis]==5.4.0
flower==2.0.1          # Celery monitoring UI (optional)

# Alerts / retry
tenacity==8.3.0

# Camera sync
apscheduler==3.10.4

# Integration tests
pytest-asyncio==0.23.6
httpx==0.27.0
Pillow==10.3.0

# DB driver needed by healthcheck + Celery tasks
asyncpg==0.29.0
